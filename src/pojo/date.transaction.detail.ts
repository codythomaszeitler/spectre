import { TransactionDetail } from "./transaction.detail";


export class DateTransactionDetail extends TransactionDetail {

    constructor(element : string, type : string, columnName : string) {
        super(element, type, columnName);
    }

    // getElement() {

    // }

    asGivenType() {

    }
}