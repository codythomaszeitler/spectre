import { Transaction } from '../../pojo/transaction';
import { Category } from '../../pojo/category';

export function generateCsvLine(transaction : Transaction, category? : Category) {
    return null;
}

export function estimateColumnsFrom(transaction : Transaction) {
    return null;
}

export function generateCsvLineWithHeader(transaction : Transaction, category? : Category) {
    return null;
}

